import { LegalLayoutWrapper } from "@/components/legal"

const LegalLayout = ({ children }: { children: React.ReactNode }) => {
  return <LegalLayoutWrapper>{children}</LegalLayoutWrapper>
}

export default LegalLayout
